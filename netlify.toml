[build]
  command = "cd web && npm install && VITE_TILES_URL=https://jgc514.github.io/taxmap/tiles npm run build"
  publish = "web/dist"

[context.production]
  command = "cd web && npm install && VITE_TILES_URL=https://jgc514.github.io/taxmap/tiles npm run build"
  publish = "web/dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[dev]
  command = "cd web && npm install && npm run dev"
  port = 5173
